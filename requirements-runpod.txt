# Kriti LMS — RunPod GPU environment (Slice 2)
# Use with:  source /workspace/kriti_lms/.venv/bin/activate
# Torch MUST be installed from the PyTorch CUDA index first (see docs/RUNPOD_ENV.md).
# Do NOT `pip install -r requirements.txt` alone — that pulls CPU onnxruntime and skips CUDA torch.

# --- already installed separately from https://download.pytorch.org/whl/cu128 ---
# torch==2.8.0+cu128
# torchvision==0.23.0+cu128
# torchaudio==2.8.0+cu128

# Diffusion / HF stack (compatible with torch 2.8)
diffusers>=0.32.0,<0.36.0
transformers>=4.46.0,<4.52.0
accelerate>=1.1.0,<1.5.0
safetensors>=0.4.0
huggingface-hub>=0.26.0

# ONNX GPU for rembg (install onnxruntime-gpu; never onnxruntime CPU on this host)
onnxruntime-gpu>=1.20.0,<1.23.0

# App requirements (from requirements.txt — excluding manim and CPU onnxruntime)
edge-tts>=6.1.9
google-genai>=0.1.1
anthropic>=0.20.0
openai>=1.0.0
python-dotenv>=1.0.1
pypdf>=4.0.0
pymupdf>=1.24.0
json-repair>=0.28.0
rembg>=2.0.50
pillow>=10.0.0
moviepy>=1.0.3

# Lip-sync / audio helpers (commented in requirements.txt; needed for Wav2Lip path)
librosa>=0.10.0
numba>=0.59.0
opencv-python-headless>=4.8.0
numpy>=1.26.0,<2.3.0
batch-face>=1.4.0
soundfile>=0.12.0
